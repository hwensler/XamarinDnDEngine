<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App11.Views.BattleDetailPage">
    <StackLayout>
        <ListView x:Name="ItemsListView"
				ItemsSource="{Binding}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding}"
                                   LineBreakMode="NoWrap"
                                   FontSize="12"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView x:Name="PostView"
				ItemsSource="{Binding}" 
                VerticalOptions = "LayoutOptions.Fill"
                BackgroundColor =" #848484">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding}"
                                   LineBreakMode="WordWrap"
                                   FontSize="20"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Next" Clicked="battleStart" />
    </StackLayout>
</ContentPage>